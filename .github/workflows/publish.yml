name: Publish Package to npm

on:
    release:
        types: [published] # Triggers when you create a GitHub release

jobs:
    publish:
        runs-on: ubuntu-latest
        permissions:
            id-token: write # CRITICAL: Required for OIDC trusted publishing authentication
            contents: read
        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Set up Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: "24"
                  registry-url: "https://registry.npmjs.org"

            - name: Install dependencies
              run: npm ci

            - name: Build
              run: npm run build

            - name: Publish to npm
              run: npm publish --provenance # Provenance links the package build back to this exact GH Action
